<?xml version="1.0" encoding="utf-8"?>
<!-- iOS-style vertical layout: Title in its own row, cards in separate row -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="16dp"
    android:paddingVertical="12dp"
    android:background="?attr/selectableItemBackground">

    <!-- Title in its own row - matches iOS -->
    <TextView
        android:id="@+id/textTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:textAppearance="?attr/textAppearanceTitleMedium"
        android:textColor="?attr/colorOnSurface"
        android:accessibilityHeading="true"
        android:importantForAccessibility="yes"
        tools:text="Tables" />

    <!-- Cards in separate row, right-aligned like iOS -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end">
        
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/tablePreviewRecyclerView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:clipToPadding="false"
            android:nestedScrollingEnabled="false"
            android:paddingHorizontal="4dp"
            android:contentDescription="Table collapse setting options"
            android:accessibilityTraversalBefore="@+id/textTitle"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            android:orientation="horizontal"
            tools:listitem="@layout/item_table_preview_card"
            tools:itemCount="2" />
            
    </LinearLayout>

</LinearLayout>