package com.omiyawaki.osrswiki.database

// import androidx.room.migration.Migration // Uncomment when specific migrations are added
// import androidx.sqlite.db.SupportSQLiteDatabase // Uncomment when specific migrations are added

/**
 * Contains Room database migrations for the application.
 *
 * Migrations should be defined as `val MIGRATION_X_Y = object : Migration(X, Y) { ... }`
 * and then added to the Room.databaseBuilder() in AppDatabase.kt.
 *
 * This file serves as a central place for all migration definitions.
 * Phase B will populate this with actual migrations (e.g., MIGRATION_6_7, MIGRATION_7_8).
 */
object DatabaseMigrations {
    // Example:
    // val MIGRATION_1_2 = object : Migration(1, 2) {
    //     override fun migrate(database: SupportSQLiteDatabase) {
    //         database.execSQL("ALTER TABLE your_table_name ADD COLUMN new_column_name TEXT")
    //     }
    // }

    // Actual migrations (MIGRATION_6_7, MIGRATION_7_8) will be added here during Phase B.
}
